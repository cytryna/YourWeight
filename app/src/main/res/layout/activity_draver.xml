<android.support.v4.widget.DrawerLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/drawer_layout"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="#ffffffff"
        android:paddingBottom="@dimen/activity_vertical_margin"
        android:paddingLeft="@dimen/activity_horizontal_margin"
        android:paddingRight="@dimen/activity_horizontal_margin"
        android:paddingTop="@dimen/activity_vertical_margin"
        tools:context=".MainActivity">

        <ListView
            android:id="@+id/listView"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:layout_alignParentStart="true"
            android:layout_alignParentTop="true" />

        <Button
            android:text="+"
            android:textColor="#FFFFFF"
            android:textSize="30sp"

            android:layout_width="100dp"
            android:layout_height="100dp"
            android:background="@drawable/plus_button"
            android:shadowColor="#A8A8A8"
            android:shadowDx="0"
            android:shadowDy="0"
            android:shadowRadius="5"
            android:id="@+id/plusButton"
            android:layout_alignParentBottom="true"
            android:layout_alignParentEnd="true" />


    </RelativeLayout>


    <!-- The first child in the layout is for the main Activity UI-->
    <!--<TableLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@+id/change_prefs_btn"
        android:layout_centerHorizontal="true"
        android:gravity="center_horizontal" >

        <TableRow>
            <TextView android:text="Światło:" />
            <TextView
                android:id="@+id/light"
                android:text="włączone" />
        </TableRow>

        <TableRow>
            <TextView android:text="Pralka:" />
            <TextView
                android:id="@+id/washing_machine"
                android:text="wyłączona" />
        </TableRow>

        <TableRow>
            <TextView android:text="Imię:" />
            <TextView
                android:id="@+id/name"
                android:text="Adam" />
        </TableRow>

        <TableRow>
            <TextView android:text="Zwierzątko:" />
            <TextView
                android:id="@+id/animal"
                android:text="kot" />
        </TableRow>

        <TableRow>
            <TextView
                android:layout_marginRight="10dp"
                android:text="Smak lodów:" />
            <TextView
                android:id="@+id/ice_cream_flavour"
                android:text="czeladowe" />
        </TableRow>

        <TableRow>
            <TextView android:text="Dzwonek:" />
            <TextView
                android:id="@+id/ringtone"
                android:text="Aserehe" />
        </TableRow>
    </TableLayout>-->

    <!-- Side navigation drawer UI -->
    <ListView
        android:id="@+id/navList"
        android:layout_width="200dp"
        android:layout_height="match_parent"
        android:layout_gravity="left|start"
        android:background="#ffeeeeee" />

</android.support.v4.widget.DrawerLayout>